# Client-side environment variables (must be prefixed with VITE_)
VITE_APP_TITLE=React TanStack Starter Template

# Disable mock API errors (set to "true" to disable random errors)
VITE_DISABLE_MOCK_ERRORS=true
